apiVersion: v1
kind: Service
metadata:
  name: prometheus
spec:
  selector:
    app: prometheus
  ports:
    - protocol: TCP
      port: {{ .Values.service.prometheusPort }}
      targetPort: {{ .Values.service.prometheusPort }}
